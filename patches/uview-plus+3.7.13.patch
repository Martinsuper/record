diff --git a/node_modules/uview-plus/components/u-datetime-picker/u-datetime-picker.vue b/node_modules/uview-plus/components/u-datetime-picker/u-datetime-picker.vue
index 7b12fe9..98ee325 100644
--- a/node_modules/uview-plus/components/u-datetime-picker/u-datetime-picker.vue
+++ b/node_modules/uview-plus/components/u-datetime-picker/u-datetime-picker.vue
@@ -7,7 +7,10 @@
 				<up-input
 					:readonly="!!showByClickInput"
 					v-model="inputValue"
-					v-bind="inputPropsInner"
+					:border="inputBorder"
+					:placeholder="placeholder"
+					:disabled="disabled"
+					:disabledColor="disabledColor"
 				></up-input>
 				<cover-view class="input-cover">
 				</cover-view>
diff --git a/node_modules/uview-plus/components/u-picker/u-picker.vue b/node_modules/uview-plus/components/u-picker/u-picker.vue
index 625e8db..40d030e 100644
--- a/node_modules/uview-plus/components/u-picker/u-picker.vue
+++ b/node_modules/uview-plus/components/u-picker/u-picker.vue
@@ -9,7 +9,8 @@
 				v-if="!$slots['default'] && !$slots['$default'] && !$slots['trigger']"
 				:readonly="true"
 				v-model="inputLabel"
-				v-bind="inputPropsInner">
+				:border="inputBorder"
+				:placeholder="placeholder">
 			</up-input>
 			<cover-view class="input-cover"></cover-view>
 		</view>
diff --git a/node_modules/uview-plus/components/u-swipe-action-item/index.wxs b/node_modules/uview-plus/components/u-swipe-action-item/index.wxs
index 6bdbc10..0a581d1 100644
--- a/node_modules/uview-plus/components/u-swipe-action-item/index.wxs
+++ b/node_modules/uview-plus/components/u-swipe-action-item/index.wxs
@@ -36,13 +36,14 @@ function touchmove(event, ownerInstance) {
 	var moveY = pageY - state.startY
 	var buttonsWidth = state.buttonsWidth
 
-	// 移动的X轴距离大于Y轴距离，也即终点与起点位置连线，与X轴夹角小于45度时，禁止页面滚动
-	if (Math.abs(moveX) > Math.abs(moveY) || Math.abs(moveX) > state.threshold) {
+	// 先判断滑动方向：如果垂直移动大于水平移动，认为是页面滚动，不触发swipe-action
+	if (Math.abs(moveX) < Math.abs(moveY)) return
+
+	// 只有水平移动大于阈值时，才阻止页面滚动并触发swipe-action
+	if (Math.abs(moveX) > state.threshold) {
 		event.preventDefault && event.preventDefault()
 		event.stopPropagation && event.stopPropagation()
 	}
-	// 如果移动的X轴距离小于Y轴距离，也即终点位置与起点位置连线，与Y轴夹角小于45度时，认为是页面上下滑动，而不是左右滑动单元格
-	if (Math.abs(moveX) < Math.abs(moveY)) return
 
 	// 限制右滑的距离，不允许内容部分往右偏移，右滑会导致X轴偏移值大于0，以此做判断
 	// 此处不能直接return，因为滑动过程中会缺失某些关键点坐标，会导致错乱，最好的办法就是
diff --git a/node_modules/uview-plus/components/u-swipe-action-item/other.js b/node_modules/uview-plus/components/u-swipe-action-item/other.js
index a68fd50..8e3d675 100644
--- a/node_modules/uview-plus/components/u-swipe-action-item/other.js
+++ b/node_modules/uview-plus/components/u-swipe-action-item/other.js
@@ -68,13 +68,14 @@ export default {
 			var moveX = pageX - this.state.startX
 			var moveY = pageY - this.state.startY
 
-			// 移动的X轴距离大于Y轴距离，也即终点与起点位置连线，与X轴夹角小于45度时，禁止页面滚动
-			if (Math.abs(moveX) > Math.abs(moveY) || Math.abs(moveX) > this.threshold) {
+			// 先判断滑动方向：如果垂直移动大于水平移动，认为是页面滚动，不触发swipe-action
+			if (Math.abs(moveX) < Math.abs(moveY)) return
+
+			// 只有水平移动大于阈值时，才阻止页面滚动并触发swipe-action
+			if (Math.abs(moveX) > this.threshold) {
 				event.preventDefault && event.preventDefault()
 				event.stopPropagation && event.stopPropagation()
 			}
-			// 如果移动的X轴距离小于Y轴距离，也即终点位置与起点位置连线，与Y轴夹角小于45度时，认为是页面上下滑动，而不是左右滑动单元格
-			if (Math.abs(moveX) < Math.abs(moveY)) return
 
 			// 限制右滑的距离，不允许内容部分往右偏移，右滑会导致X轴偏移值大于0，以此做判断
 			// 此处不能直接return，因为滑动过程中会缺失某些关键点坐标，会导致错乱，最好的办法就是
